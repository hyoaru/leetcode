[project]
name = "leetcode"
version = "0.1.0"
readme = "README.md"

requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "notebook>=7.6.2",
]

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false